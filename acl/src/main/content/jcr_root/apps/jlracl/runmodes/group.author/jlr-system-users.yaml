- DEF systemUserPath="/home/users/system/jlr"
- user_config:

    - vehicle-card-service:

        - name: "System user for Vehicle card"
          isMemberOf: global-authors
          path: ${systemUserPath}
          isSystemUser: "true"

    - jlr-pricing-user:

        - name: "JLR Pricing User"
          path: ${systemUserPath}
          isSystemUser: "true"

- ace_config:

  - jlr-pricing-user:

    - path: /content/landrover
      permission: allow
      actions: read

    - path: /var/jlr
      permission: allow
      actions: read,create,modify,delete,replicate